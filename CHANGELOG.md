# Changelog

## Unreleased

- 规范项目目录：源码迁移到 `MOrangeCompanion/`，文档素材进入 `docs/`，发布元数据进入 `release/`，构建脚本进入 `scripts/`。
- 将 Xcode 工程和 target 标识统一为 `MOrangeCompanion`。
- 将早期工程文件名改为小橘子命名。
- 移除仓库中的构建产物目录 `build/`。
- 更新 README、素材规范、发布说明和致谢说明。
